#include <stdio.h>


// police 4 exercise, 
// murder boulevard is  L meters long, 
// position Wiliams position is x = 0;
// nest position x = L;
// in this street there are N traffic lights at position X_i,
// they are all synchronized at t = 0;
// green when triggered will stay green for T seconds,
// the same thing happens for red;
//
// william is travelling at the speed of 1 m/s, he will cross
// the street with a red semaphore only R times;
//
// N, R, T, L
// X[0], X[1], ... X[N]
